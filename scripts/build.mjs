import {rm,mkdir,cp,writeFile} from 'node:fs/promises';import {join} from 'node:path';const out='dist';await rm(out,{recursive:true,force:true});await mkdir(out,{recursive:true});await cp('frontend',join(out,'frontend'),{recursive:true});await cp('backend',join(out,'backend'),{recursive:true});await writeFile(join(out,'BUILD_OK.txt'),`Build generated ${new Date().toISOString()}\n`);console.log('Build complete:',out);