# Build

Canonical boundary for export, compression, bundling, cache/version metadata and deployment preparation. Production build tooling must consume canonical modules and must not introduce gameplay logic.
