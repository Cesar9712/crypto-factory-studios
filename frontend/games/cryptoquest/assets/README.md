# Assets

Canonical asset catalog boundary. Models, animations, textures, icons, sounds and data files should be referenced through stable asset identifiers rather than hard-coded DOM paths. Existing legacy assets remain compatible during migration.
