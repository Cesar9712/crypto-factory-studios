const q=new URLSearchParams(location.search),slug=q.get('slug');const root=document.getElementById('gameHero');
(async()=>{if(!slug){root.innerHTML='<h1>Juego no encontrado</h1>';return}try{const r=await fetch('/api/v1/games/'+encodeURIComponent(slug));const d=await r.json();if(!r.ok)throw new Error('No disponible');const g=d.game;document.title=g.title+' · Crypto Factory Studios';root.innerHTML=`<div class="game-detail-art"><span class="status live">AVAILABLE</span><span class="art-number">CFS</span></div><div class="game-detail-copy"><span class="eyebrow">${g.genre}</span><h1>${g.title}</h1><p>${g.description||'Juego publicado en Crypto Factory Studios.'}</p><div class="tag-row">${(g.tags||[]).map(x=>`<span class="chip">${x}</span>`).join('')}</div><p class="muted">Por ${g.creator_name}</p><a class="primary big" href="${d.play_url}">▶ JUGAR AHORA</a></div>`}catch(e){root.innerHTML='<span class="eyebrow">GAME ROOM</span><h1>Este juego no está disponible.</h1><a class="ghost" href="/#games">Volver a juegos</a>'}})();
