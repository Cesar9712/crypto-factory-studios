const q=new URLSearchParams(location.search),slug=q.get('slug');const root=document.getElementById('gameHero');
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
(async()=>{if(!slug){root.innerHTML='<h1>Juego no encontrado</h1>';return}try{const r=await fetch('/api/v1/games/'+encodeURIComponent(slug));const d=await r.json();if(!r.ok)throw new Error('No disponible');const g=d.game;document.title=g.title+' · Crypto Factory Studios';const safePlay=typeof d.play_url==='string'&&d.play_url.startsWith('/play/')?d.play_url:'#';root.innerHTML=`<div class="game-detail-art"><span class="status live">AVAILABLE</span><span class="art-number">CFS</span></div><div class="game-detail-copy"><span class="eyebrow">${esc(g.genre)}</span><h1>${esc(g.title)}</h1><p>${esc(g.description||'Juego publicado en Crypto Factory Studios.')}</p><div class="tag-row">${(g.tags||[]).map(x=>`<span class="chip">${esc(x)}</span>`).join('')}</div><p class="muted">Por ${esc(g.creator_name)}</p><a class="primary big" href="${esc(safePlay)}">▶ JUGAR AHORA</a></div>`}catch(e){root.innerHTML='<span class="eyebrow">GAME ROOM</span><h1>Este juego no está disponible.</h1><a class="ghost" href="/#games">Volver a juegos</a>'}})();