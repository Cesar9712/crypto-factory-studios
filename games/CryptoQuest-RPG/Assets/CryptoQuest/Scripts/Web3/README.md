# thirdweb integration

Target SDK: thirdweb Unity SDK v6.1.3.
Test network: Base Sepolia (chain ID 84532).
Production network will remain disabled until testnet validation is complete.

The Unity client uses the public Client ID only. Never commit a thirdweb Secret Key, wallet private key, or seed phrase.
