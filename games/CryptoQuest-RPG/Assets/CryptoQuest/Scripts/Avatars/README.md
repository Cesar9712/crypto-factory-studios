# Avatar integration

Provider-neutral avatar integration lives here.

Ready Player Me hosted services are no longer callable, so do not add runtime dependencies on its retired API. Existing legally exported GLB assets may be imported as normal Unity assets. A replacement provider can be added behind an adapter later.
